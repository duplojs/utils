Creates a NewType value and returns an Either.
On validation failure, the left value contains the NewType name and the underlying DataParser error.

```ts
{@include clean/createNewType/example.ts[13,13]}
```
