Creates a primitive value and returns an Either.
On validation failure, the left value contains the primitive name and the underlying DataParser error.

```ts
{@include clean/primitive/example.ts[3,7]}
```
