Creates a primitive value from the handler's wider input type and throws on error.

When the handler has no wider input type, it accepts the primitive value type.

```ts
{@include clean/primitive/example.ts[31,32]}
```
