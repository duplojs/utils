Creates a constrained value and returns an Either.
On validation failure, the left value contains the constraint name and the underlying DataParser error.

```ts
{@include clean/createConstraint/example.ts[12,16]}
```
