Returns the length of a string.

Signature: `length(input)` → returns the string length

The input string is not mutated.

```ts
{@include string/length/example.ts[3,11]}
```

@see https://utils.duplojs.dev/en/v1/api/string/length

@namespace S
