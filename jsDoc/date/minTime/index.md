Returns the smallest time from an iterable.

Signature: `minTime(input)` → returns a value

The input value is not mutated.

```ts
{@include date/minTime/example.ts[3,20]}
```

@see https://utils.duplojs.dev/en/v1/api/date/minTime

@namespace D
