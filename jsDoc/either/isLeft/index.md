Type guard that checks whether a value is an EitherLeft. Ideal to secure an unwrap or trigger an error branch.

Signature: `isLeft(input)` → returns a value

Acts as a type guard and narrows the input type when true.

```ts
{@include either/isLeft/example.ts[3,9]}
```

@see https://utils.duplojs.dev/en/v1/api/either/isLeft

@namespace E
